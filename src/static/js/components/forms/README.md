```text
- form (appearance: form-classic, form-boostrap, form-table, etc.)
- form-item (meta component to describe one form item using markup)
- form-label (meta component to describe one label using markup)
- form-control (meta component to describe one label using markup)
- form-abstract (a component which substitutes real input for abstract form_item.type)
    - form-component
    - form-renderer
    - form-mapper
    - form-resolver
    - form-input-resolver
    - form-type-component

form-types
    - form-controls
    - form-inputs

form:
    - value (where to store values)
    - an array of form_items
    - type map (a way to map type like string, or integer, or color to a real component)
```
